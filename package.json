{
  "name": "svg-to-attr-loader",
  "version": "1.0.0",
  "description": "Webpack loader that transforms all paths in an SVG file to a single path array, directly assignable to the d attribute of an SVG path DOM element",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d build",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Tradesmarter/svg-to-attr-loader.git"
  },
  "author": "Omri Gilad <omri@tradesmarter.com>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Tradesmarter/svg-to-attr-loader/issues"
  },
  "homepage": "https://github.com/Tradesmarter/svg-to-attr-loader#readme",
  "dependencies": {
    "jsdom": "^9.4.2"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2"
  }
}
